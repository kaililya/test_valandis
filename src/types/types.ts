export enum TParams {
  FILTER = "filter",
  GET_IDS = "get_ids",
  GET_ITEMS = "get_items",
  GET_FIELDS = "get_fields",
}

export type TGood = {
  brand: null | string;
  id: string,
  price: number,
  product: string
}
